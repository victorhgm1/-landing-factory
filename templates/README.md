# /templates — os moldes HTML
`landing.html` (captura) e `obrigado.html`, com marcadores `{{PLACEHOLDER}}`.
- Mudança aqui afeta TODOS os produtos — tratar como mudança estrutural.
- Placeholder novo exige o par no `map` do `scripts/build.js`, senão o build derruba (proposital).
- Tipografia fixa: Lora (títulos) + Inter (resto). Não hardcodar conteúdo de produto.
